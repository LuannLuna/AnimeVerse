query AllMangas($page: Int, $perPage: Int) {
    Page(page: $page, perPage: $perPage) {
        pageInfo {
            currentPage
            total
            hasNextPage
        }
        media(type: MANGA, sort: START_DATE_DESC) {
            id
            title { romaji english native }
            description(asHtml: false)
            startDate { year month day }
            coverImage { large }
        }
    }
}
