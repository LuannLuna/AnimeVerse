query AllAnimes($page: Int, $offSet: Int) {
    Page(page: $page, perPage: $offSet) {
    media(type: ANIME, sort: START_DATE_DESC) {
      id
      title {
        romaji
        english
        native
      }
      startDate {
        year
        month
        day
      }
      coverImage {
        large
      }
    }
  }
}
