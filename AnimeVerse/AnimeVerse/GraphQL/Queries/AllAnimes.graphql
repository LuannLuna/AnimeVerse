query AllAnimes($page: Int) {
  Page(page: $page) {
    media {
      id
      siteUrl
      title {
        english
        native
      }
      description
      coverImage {
        large
      }
    }
  }
}
