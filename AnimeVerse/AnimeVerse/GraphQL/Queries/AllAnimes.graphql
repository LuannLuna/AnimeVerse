query AllAnimes($page: Int, $perPage: Int, $sort: [MediaSort]) {
    Page(page: $page, perPage: $perPage) {
        pageInfo {
            currentPage
            total
            hasNextPage
        }
        media(type: ANIME, sort: $sort) {
            id
            title { romaji english native }
            description(asHtml: false)
            startDate { year month day }
            coverImage { large }
        }
    }
}

